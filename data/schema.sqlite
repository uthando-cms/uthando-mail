CREATE TABLE mailQueue (
  mailQueueId integer PRIMARY KEY AUTOINCREMENT NOT NULL,
  recipient text(128),
  sender text(128),
  subject varchar(128),
  body text,
  isHtml char,
  priority integer(128),
  dateCreated datetime(128)
);